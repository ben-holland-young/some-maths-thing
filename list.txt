ALGEBRA -
        DERIVATIVES math.derivative().toString()
        SIMPLIFYING/FACTORISING math.simplify().toString()
        QUADRATIC FORMULA -(b)(+-)math.sqrt(((b)^2)-(4*(a*c)))/r2*a
GEOMETRY -
        AREAS -
              SQUARE s*s
              RECTANGLE l*s
              TRIANGLE (b*h)/2
              TRAPEZOID (b1+b2)*(h/2)
              CIRCLE - CIRCUMFERENCE TOO A: pi * r^2 C: pi * (r * 2)
        VOLUMES-
                CUBE a^3
                RECTANGULAR PRISM a*b*c
                IRREGULAR PRISM b*h
                CYLINDER pi * r^2 * h
                PYRAMID (1/3) * b * h
                CONE 1/3 * pi * r^2 * h
                SPHERE (4/3) * pi * r^3
                ELLIPSOID (4/3) * pi * r1 * r2 * r3
        ANGLES-
              Triangle (if isosceles show one input and say given angle, if scalene ask for two inputs and add together and divide by two)
        PYTHAGORAS math.hypot(a,b)
        GRADIENT GRAPH NEEDS X1, Y1 AND X2, Y2 math.distance([x1,y1],[x2,y2])
TRIG -
        TRIG FUNCTIONS
ARITHMETIC -
    INDICES math.simplify(y)
    SURDS math.simplify(x)
    FACTORIAL math.factorial(n)
    AVERAGES -
            MEAN math.mean(a,b,c) seperate input by ","
            MDDE math.mode(a,b,c) seperate input by ","
            MEDIAN math.median(a,b,c) seperate input by ","
            RANGE(USE BUBBLE SORT TO DO HIGHEST - LOWEST)
            SUM math.sum(a,b,c) seperate input by ","
    UNIT CONVERSION  math.to(y,x)
    GREATEST COMMON FACTOR math.gcd(a,b)
    LOWEST COMMON MULTIPLE math.lcm(a,b)
    NTH ROOT math.nthRoot(a)
    X TO THE Y math.pow(x,y)
    SQUARE ROOT math.sqrt(x)
    SQUARE math.square(x)
    CUBE math.cube(x)
    CUBE ROOT math.cbrt(x)
    RANDOM INT math.randomInt(size, min, max)
    IS PRIME? math.isPrime(x)


